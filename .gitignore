# Claude Code IDE config — not part of the project
.claude/

# Personal task tracking — not part of the project
TODO.md

# OS-generated files
.DS_Store
Thumbs.db
desktop.ini

# Editor config/state
.vscode/
.idea/
*.swp
*.swo

# Node modules — in case someone forks and adds tooling
node_modules/

# Generated npm lockfile — project deliberately ships without a committed lockfile
package-lock.json

# Local git worktrees — created via superpowers:using-git-worktrees skill
.worktrees/
