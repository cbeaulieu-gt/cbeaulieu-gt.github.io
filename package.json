{
  "name": "github-profile-page",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "format": "prettier --write index.html",
    "check": "prettier --check index.html tests/profile.test.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
